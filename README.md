# 2026-Coding-Competition

# TODO

- obfuscate test files
    - base64 encode the python, save that encoded block to test_01.enc
    - test run decodes the base64, saves output to a python file, runs the tests, then deletes the python file